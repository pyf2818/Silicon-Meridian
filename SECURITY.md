# 安全政策

## 报告漏洞

**请不要在公开 Issue、讨论区或社交媒体上披露漏洞细节。**

本仓库使用 GitHub 的 **私密安全通告（Private vulnerability reporting）** 作为唯一上报渠道：

1. 打开仓库页面 → **Security** 标签页
2. 点击 **Report a vulnerability**
3. 填写标题、影响面、复现步骤；如需可附 PoC

提交后仅维护者可见，可在该通告内直接沟通与协作修复。

> 若该入口在你的账号下不可见，说明仓库尚未启用私密上报——请通过 GitHub 私信联系维护者
> [@pyf2818](https://github.com/pyf2818) 索取其它渠道，不要把细节写进公开 Issue。

## 请一并提供

- 受影响的版本 / commit
- 复现步骤或最小 PoC
- 影响评估：能否读取他人数据、能否越权写、是否需要已登录、是否可远程触发
- 你希望如何署名（可选）

## 处理流程

| 阶段 | 预期 |
| --- | --- |
| 确认收到 | 7 天内 |
| 初步定性（是否漏洞 / 严重度） | 14 天内 |
| 修复并发布 | 视严重度而定，修复后会在安全通告中说明 |

严重度分级参考 CVSS，优先处理：认证绕过、越权访问他人数据、服务端请求伪造（SSRF）、
密钥泄漏、任意代码/命令执行。

## 适用范围

本仓库包含三类可部署形态，报告时请注明：

- **Node 生产服务**：`npm start` → `server/productionServer.js`
- **Serverless API**：`api/*.js`（Vercel 等平台）
- **前端 SPA**：`dist/`（构建产物）

## 已内置的防护（便于你先判断是否属于「预期行为」）

- **AI / 抓取出站网关**：请求体大小限制、连接与总超时、频率限制、**私网地址拦截**、
  DNS 解析校验、重定向阻断；白名单由 `AI_ALLOWED_HOSTS` 控制，
  本地模型场景需显式设置 `AI_ALLOW_PRIVATE_NETWORK=true` 才能连私网
  （见 `server/security/urlSafety.js`）
- **认证**：不透明随机 token + 服务端 session 表（非 JWT）；Cookie
  `meridian_session` 为 `HttpOnly` + `SameSite=Lax`；密码使用 `node:crypto` 的 scrypt 哈希
- **MCP / 工具调用**：外部工具需显式审批，工具返回内容在进入模型前包裹为不可信数据
- **MCP 配置**：`mcp.config.json` 中的 `env` 可能含令牌，请勿提交任何真实密钥

## 不属于安全漏洞的常见情况

- 未配置 `DATABASE_URL` 时相关接口返回 503 —— 这是预期的降级行为
- 未配置大模型时使用内置确定性算法 —— 预期行为，非降级漏洞
- 依赖项的已知 CVE（请直接向上游报告，或开普通 Issue 提示升级）
- 缺少安全响应头、缺少 CSP 等硬化类建议（欢迎提 Issue 讨论，但不算漏洞）

## 密钥泄漏处置

如果你不慎把密钥提交进了本仓库：

1. **先撤销并轮换该密钥**（这一步比清理历史更重要）
2. 再通过上述私密渠道告知，我们会协助清理历史并检查是否已被使用

## 支持版本

项目处于活跃开发期（`0.1.x`），**仅最新 `main` 分支接受安全修复**，不维护历史版本分支。
